services:
  - type: web
    name: winmiskam-pipeline
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app/ui/streamlit_app.py --server.port $PORT --server.address 0.0.0.0 --browser.gatherUsageStats false"